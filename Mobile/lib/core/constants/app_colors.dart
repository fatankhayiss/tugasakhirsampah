import 'package:flutter/material.dart';

class AppColors {
  // GREEN SYSTEM
  static const primary = Color(0xFF49C67A);
  static const secondary = Color(0xFF2E9E5B);
  static const softGreen = Color(0xFFEAF8EF);

  // BLUE CTA
  static const primaryBlue = Color(0xFF2D5BFF);
  static const secondaryBlue = Color(0xFF4A7BFF);
  static const softBlue = Color(0xFFEAF1FB); // maintaining from previous for compatibility

  // BACKGROUND
  static const background = Color(0xFFF8FAF8);
  static const softBackground = Color(0xFFF8FAF8); // maintaining structure

  // DARK THEME (For Scan)
  static const darkBackground = Color(0xFF050505);
  static const neonGreen = Color(0xFF43C97B);

  // TEXT
  static const textDark = Color(0xFF1B1B1B);
  static const textSoft = Color(0xFF7B7B7B); // newly requested

  // BORDER
  static const border = Color(0xFFEAEAEA); // updated per prompt
}
